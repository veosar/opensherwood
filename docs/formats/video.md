# Video (`<lang>/data/Cinematics/Intro.vid`, `Outro.vid`)

Bink video (RAD Game Tools), magic `BIKi`, 640x320 (Intro), 4260 frames. The original loads `binkw32.dll`.
Bink is a well-understood format (FFmpeg has a decoder). Options: port a decoder (LGPL, GPL-compatible), or skip
cinematics with a "video not supported" notice. Low priority.

## Provenance

Observation (header).
