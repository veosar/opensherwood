# Disposition of Codex review 13 (2026-09-13)

Review: `2026-09-13-codex-review-13.md` (the governance change of ADR-0009, commit `b13372e`). Verdict there:
fix-then-merge. The review is a risk assessment, not legal advice; so is this answer.

| # | Finding | Disposition |
|---|---------|-------------|
| 1 | The mandate ("understand completely", "read everything") exceeds the interoperability justification | **Done.** ADR-0009 point 1 confines analysis to what interoperability with the player's files requires; every specification opens with a necessity record (target, information not otherwise available, scope read, stopping condition); the skill says "read what the target requires". |
| 2 | Public release of specifications treated as automatic | **Done in policy; the counsel assessment is the maintainer's action.** `docs/legal.md` gains "Necessity and disclosure": publication is a separate approval per specification (the template's identity block), with the onward-disclosure limits named; counsel assessment before a release is recorded as required. |
| 3 | Store / publisher terms undocumented | **Done in policy.** The maintainer keeps the acquisition record and the store terms privately; the policy quotes the statutory non-excludability; "lawful access to a copy" wording. |
| 4 | "Own words" is an insufficient expression filter | **Done.** The expression-filter table (paraphrase, structure, identifiers, constants, tables, algorithms) is in ADR-0009 section 5 and in the analyst skill's self-check; no granularity threshold is claimed. |
| 5 | Reviewer exception and session rules leave a route around the wall | **Done.** Two reviewer roles (spec reviewer with `re/` access whose output is spec corrections only; implementation reviewer without `re/`), fresh implementer sessions with cleared specs only, questions through spec amendments, exposure tracked per subsystem in the identity block; `.gitignore` is stated not to be access control. |
| 6 | Broken word boundaries (literal backspace bytes) in the gate | **Done.** The script is rewritten; the committed file at `b13372e` indeed carried five backspace bytes (heredoc mangling); `--selftest` now runs synthetic fragments through the detectors and refuses control characters in patterns. |
| 7 | False negatives (`.rep` directories, `.gar`, UTF-16, size skip, missed idioms) and false positives | **Done.** Ancestor `.rep` / `.gpr` directories, `.gar` / `.prp`, UTF-16 decoding, more idiom families and exporter signatures (hard fail), no silent size skip (files above the inspection limit are reported), two-family threshold with a single-family tolerance for prose; exemptions are exact paths. |
| 8 | The gate inspects the working tree, not what is published | **Done.** Default mode inspects the index blobs; `--range A..B` inspects every blob added or changed by the commits of a push (`scripts/hooks/pre-push`, `core.hooksPath`); `--paths` inspects drafts. |
| 9 | Export destinations unrestricted | **Done.** Every export script resolves the canonical output path and refuses it unless it lies under a directory named `re`; the skill says to keep logs there; the DecompileAll export used for this session wrote under `re/out`. |
| 10 | `ExportInventory` did not compile at `b13372e` | **Done (`4948af1`).** |
| 11 | Failed or partial exports look successful | **Partly done.** Decompiler open failure is an error, disposal is in `finally`, cancellation is an error, failures are counted and printed; manifests with build hash and completeness, reversible string escaping and call-site counts are open (tooling, low risk). |
| 12 | The template lacks an evidence and acceptance contract | **Done.** The template has a necessity record, an identity block (build, edition, addresses' base, analyst / reviewer sessions, publication approval), a claims table (id, status, evidence, confidence, contradictions), execution-semantics requirements, acceptance tests, implementation choices. |
