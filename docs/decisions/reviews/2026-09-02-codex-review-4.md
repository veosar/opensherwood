# Codex adversarial review 4: commits 3c148c5 and 3828c3b (2026-09-02)

Requested by the lead with the cross-agent-review skill; disposition in
`2026-09-02-codex-review-4-disposition.md`.

## Findings

1. **Critical — Review 3’s text sweep is incomplete, and `3828c3b` reintroduces prohibited game data.** The repository now contains asset-derived lookup tables and exact designer strings in [profile.md:48](</C:/Users/przem/source/repos/opensherwood/docs/formats/profile.md:48>), [profile.md:73](</C:/Users/przem/source/repos/opensherwood/docs/formats/profile.md:73>), and [profile.md:109](</C:/Users/przem/source/repos/opensherwood/docs/formats/profile.md:109>); purportedly synthetic CPF fixtures copy those names and labels at [cpf.rs:413](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-formats/src/cpf.rs:413>) and [cpf.rs:456](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-formats/src/cpf.rs:456>). SCB documentation also includes literal executable messages, an original build-machine path, callback/name catalogs, and mission element names at [scb.md:10](</C:/Users/przem/source/repos/opensherwood/docs/formats/scb.md:10>), [scb.md:30](</C:/Users/przem/source/repos/opensherwood/docs/formats/scb.md:30>), and [scb.md:51](</C:/Users/przem/source/repos/opensherwood/docs/formats/scb.md:51>). Older catalogs remain too, for example [data-inventory.md:40](</C:/Users/przem/source/repos/opensherwood/docs/original/data-inventory.md:40>). This conflicts directly with [ADR-0003:39](</C:/Users/przem/source/repos/opensherwood/docs/decisions/ADR-0003-clean-room-roles.md:39>) and `docs/legal.md`. Replace catalogs with counts, indices, hashes, aggregate ranges, and paraphrases; use invented fixture strings; derive necessary runtime resource names from user data. The newly pushed history now needs the same maintainer disposition as the earlier leak.

2. **High — Hidden/activated actors have no authoritative `active` state.** The parser and spec explicitly identify initially hidden PCs activated by scripts at [rhm.rs:71](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-formats/src/rhm.rs:71>) and [rhm.md:103](</C:/Users/przem/source/repos/opensherwood/docs/formats/rhm.md:103>), but mission construction ignores the placement flag at [mission.rs:273](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/mission.rs:273>) and every entity starts `alive: true` at [world.rs:491](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/world.rs:491>). Consequently hidden actors render, select, and simulate immediately. Programs, program counters, `alive`, and geometry areas are correctly hashed at [world.rs:1211](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/world.rs:1211>), [world.rs:1231](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/world.rs:1231>), [world.rs:1233](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/world.rs:1233>), and [world.rs:1296](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/world.rs:1296>), but there is no active flag to cover. Add a distinct `active` field through placement/spec/entity/snapshot/validation/hash and filter selection, simulation, and rendering; do not overload death state.

3. **High — The replay recorder can produce a replay its own parser rejects and can allocate well beyond the 64 MiB policy.** Recording checks events, checkpoints, and ticks at [engine.rs:541](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/engine.rs:541>), but never accumulated encoded bytes. Up to 2²⁰ verbose events can exceed the parser’s `MAX_BYTES` at [lib.rs:14](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-protocol/src/lib.rs:14>); `replay.stop` then materializes the entire result infallibly at [lib.rs:527](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-protocol/src/lib.rs:527>) and [engine.rs:1170](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/engine.rs:1170>). Track serialized bytes before mutating a recording, use a bounded/fallible writer, and test worst-case event representations at the cap.

4. **High — Content identity can remain unchanged after content changes.** The fingerprint cache trusts only path, size, and mtime at [lib.rs:300](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-assets/src/lib.rs:300>), so a same-size edit with a preserved or coarse timestamp returns the stale digest. Added files are also invisible until `GameDir` is reopened because the layer index is built once. There is no post-read metadata/identity comparison for same-size concurrent replacement. This contradicts the “any edited, added, removed or replaced byte” guarantee at [lib.rs:269](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-assets/src/lib.rs:269>). Rehash for every identity-sensitive operation, or rescan and use a robust immutable-file identity with pre/post checks and retry; add preserved-mtime and concurrent-replacement tests.

5. **High — Geometry limits prevent arithmetic overflow but not hostile restore CPU/memory exhaustion.** `MAX_GEOMETRY_VERTICES` permits 2²⁰ vertices at [world.rs:343](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/world.rs:343>), while rasterization scans every grid row for every polygon edge at [nav.rs:320](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/nav.rs:320>). Compact inline JSON can therefore request billions of edge tests or hundreds of thousands of polygons. Worse, restore replaces the world before rebuilding navigation at [world.rs:1129](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/world.rs:1129>), so the “transactional” boundary excludes the expensive/fallible derived-state construction. Bound polygon count and raster work, scan only polygon y-bounds, use fallible allocations, and build the new `NavGrid` before committing the world.

6. **High — A hostile program can panic validation in debug and be accepted in release.** [world.rs:779](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/world.rs:779>) calls `i32::abs()` on `Turn.delta256`; `i32::MIN.abs()` overflows. Debug builds panic, while optimized builds can leave `i32::MIN`, making the `< 256` check pass. Use `unsigned_abs() < 256` or a widened integer and add an inline-JSON restore test for `i32::MIN` in both profiles.

7. **High — Input and recording state have undefined semantics across restore and RPC EOF.** Same-scenario restore only replaces the world at [engine.rs:1064](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/engine.rs:1064>), retaining queued OS input and an active recorder. Restoring to an earlier tick can therefore append out-of-order replay events, yielding a self-generated invalid replay, while stale input is applied to the restored world. Separately, window input is moved into `Session::queued_input`, but RPC disconnect merely switches to autonomous mode at [window.rs:382](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/window.rs:382>); autonomous ticks consume only `App.pending` at [window.rs:417](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/window.rs:417>), stranding the queued events. Reject restore during recording or terminate recording on success, define and enforce queue clearing, and transfer queued session input back into the first autonomous tick after EOF.

8. **Medium — Pause/HUD state is hardwired to the first mission and depends on session history.** Only `start_campaign()` initializes the objective and HUD at [engine.rs:246](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/engine.rs:246>), using first-mission text plus invented `money = 100` and `clover = 0` at [engine.rs:263](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/engine.rs:263>). Direct `mission:*` resets leave this state empty or stale, and Pause→Restart calls `start_campaign()` at [engine.rs:385](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/engine.rs:385>), replacing any current mission with H01 and showing its briefing. The portrait is also permanently the first portrait at [ui_assets.rs:174](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/ui_assets.rs:174>). Derive/clear presentation state on every install, restart the current scenario and seed, and hide counters until authoritative campaign state exists.

9. **Medium — Retail scenario loading remains fail-open.** Missing or malformed RHP data logs and returns default geometry at [engine.rs:669](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/engine.rs:669>); empty geometry makes the entire navigation grid walkable at [nav.rs:63](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/nav.rs:63>). Missing/corrupt CPF and referenced sprite profiles similarly become defaults at [mission.rs:141](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/mission.rs:141>), while sprite-bank/catalog failures produce an empty or partial catalog at [engine.rs:731](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/engine.rs:731>). Catalog-relative snapshot validation is then skipped whenever the catalog is empty at [world.rs:837](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-core/src/world.rs:837>). Required retail dependencies should fail scenario construction; reserve fallback behavior for an explicit diagnostic mode.

10. **Medium — RGBA materialization remains outside the new allocation budgets.** Sprite conversion uses unchecked multiplication and infallible allocation at [sprite_decode.rs:353](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-formats/src/sprite_decode.rs:353>), called by the bank at [sprites.rs:161](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-assets/src/sprites.rs:161>). UI conversion repeats the pattern at [ui_assets.rs:86](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/ui_assets.rs:86>); an archive within the 256 MiB decoded SRES budget can require another roughly 512 MiB in RGBA copies. Make conversions fallible, validate `width × height == pixels.len()`, use checked multiplication/`try_reserve_exact`, and charge final materialized bytes to a cumulative budget.

11. **Medium — Walkable projection areas deliberately connect unrelated elevations.** Every WOAW polygon is flattened to x/y and unioned into one walkable set at [engine.rs:699](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/engine.rs:699>), ignoring z, layer membership, links, and `007` bonds. The spec itself admits that touching or overlapping polygons become connected across layers at [rhp.md:94](</C:/Users/przem/source/repos/opensherwood/docs/formats/rhp.md:94>) and notes that some small areas sit on rocks/bushes at [rhp.md:126](</C:/Users/przem/source/repos/opensherwood/docs/formats/rhp.md:126>). Keep this behind an explicitly approximate-navigation mode or model per-area connectivity/elevation before advertising town navigation as campaign-capable; add stacked/overlapping-area fixtures.

12. **Medium — FACE records lose most of their depth semantics at the app/render boundary.** FACE commonly has two polylines and optional area references at [rhp.md:141](</C:/Users/przem/source/repos/opensherwood/docs/formats/rhp.md:141>) and [rhp.md:159](</C:/Users/przem/source/repos/opensherwood/docs/formats/rhp.md:159>), but the app takes only the first line’s first two points at [engine.rs:683](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-app/src/engine.rs:683>) and discards references/layers. Which line applies is not established, so selecting the first is an undocumented semantic guess. Preserve all lines and references in the render model and oracle-test multi-line masks before selecting a rule.

13. **Medium — Renderer public APIs are still not total on hostile inputs.** `Occluder::covers` subtracts arbitrary `i32`s at [lib.rs:62](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-render/src/lib.rs:62>), `depth_y` performs overflowing `i32` differences/additions at [lib.rs:75](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-render/src/lib.rs:75>), and blit length/position arithmetic is unchecked at [lib.rs:302](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-render/src/lib.rs:302>) and [lib.rs:341](</C:/Users/przem/source/repos/opensherwood/crates/opensherwood-render/src/lib.rs:341>). Debug and release can diverge for public, synthetically constructed values. Widen coordinate math, use checked buffer-size calculations, and test `i32`/`u32` extremes.

14. **Medium — `play_window.py` can pass without exercising the claimed OS input path and is not reliably targeted to the child process.** It selects the first window with the title rather than matching the engine PID at [play_window.py:33](</C:/Users/przem/source/repos/opensherwood/harness/tools/play_window.py:33>), checks overlap only once at construction at [play_window.py:79](</C:/Users/przem/source/repos/opensherwood/harness/tools/play_window.py:79>), and does not refresh window position/client dimensions in `refresh()` at [play_window.py:89](</C:/Users/przem/source/repos/opensherwood/harness/tools/play_window.py:89>). When modifiers are held, keys silently become canonical RPC events while the flow can still pass at [play_window.py:110](</C:/Users/przem/source/repos/opensherwood/harness/tools/play_window.py:110>). Bind the HWND to the spawned PID, revalidate foreground/visibility/client geometry before every action, and mark RPC fallback as skipped/degraded rather than an OS-E2E pass.

15. **Medium — SCB provenance is generally data-derived and well recorded, but the walkthrough outruns its dependency confidence.** The corpus/probe provenance at [scb.md:397](</C:/Users/przem/source/repos/opensherwood/docs/formats/scb.md:397>) is substantially better than the earlier format notes and clearly separates data-file work from executable strings. However the native table contains medium/low hypotheses, while the first-mission walkthrough turns those into unqualified factual behavior and even says the flow matches “exactly” at [scb.md:283](</C:/Users/przem/source/repos/opensherwood/docs/formats/scb.md:283>) and [scb.md:300](</C:/Users/przem/source/repos/opensherwood/docs/formats/scb.md:300>). Annotate walkthrough statements with the minimum confidence of every opcode/native/index-space dependency and reserve “exact” for oracle-confirmed traces.

16. **Medium — ADR-0008’s ownership direction is sound, but “unknown native = no-op” is unsafe architecture.** [ADR-0008:22](</C:/Users/przem/source/repos/opensherwood/docs/decisions/ADR-0008-script-vm-ownership.md:22>) lets execution continue after an unknown native and only promises a load-time/observable count. Unknown calls may determine return values, branches, activation, or scheduling, so a no-op can silently produce an internally valid but false mission state; the ADR also does not explicitly place runtime unknown-call history in snapshot/hash state. Default to a deterministic unsupported-native trap or mission-load refusal. If a permissive diagnostic mode is retained, record ordered calls and arguments in authoritative snapshotted/hashed state.

17. **Low — Documentation and hygiene are already drifting.** [status report:28](</C:/Users/przem/source/repos/opensherwood/docs/status/2026-09-02.md:28>) says P1 items 14/16/17 remain open while the updated disposition marks them done, and simultaneously claims Review 3’s text sweep is addressed. [status report:35](</C:/Users/przem/source/repos/opensherwood/docs/status/2026-09-02.md:35>) says the HUD has no counters although the renderer draws fabricated counters. `git diff --check 3279445..HEAD` also fails on an added blank line at [image-blob.md:50](</C:/Users/przem/source/repos/opensherwood/docs/formats/image-blob.md:50>). Run the docs-sync checklist after correcting the implementation and dispositions.

The CPF grammar’s count/string bounds and exact-consumption checks look sound. The RPC oversized-line drain, SRES parse-time cumulative budgets, versioned snapshot envelope, deterministic program ordering, program/area hash coverage, and the basic per-sprite masking approach also held up under static review.

Verification: `cargo fmt --check`, `scripts/check_no_assets.py`, and `scripts/sync_skills.py --check` passed; the asset checker explicitly cannot detect copied text. Full Rust and pytest runs were blocked by the read-only environment (`target/debug/.cargo-lock` and no writable temporary directory), and no game-data tests were available. The required Claude cross-review was attempted, but its CLI connection was refused.

**Verdict: fix-then-merge.** The ownership architecture does not require wholesale redesign, but the clean-room regression and High-severity determinism/resource issues are merge blockers.