# Disposition of Codex review 9 (2026-09-05)

Review: `2026-09-05-codex-review-9.md` (commits `dc9f006..842407e`). Verdict there: redesign, on the taint
model's coverage of engine-side hypotheses, the phase fairness of the simulation budget, and the history gate.

| # | Finding | Disposition |
|---|---------|-------------|
| 1 | Engine hypotheses (exact noise radius, sight, alert timing, attack movement, knock-out) mutate state without an assumption unless an `ActionChange` handler runs | **Accepted, open (core batch 2, after the combat batch in progress).** Assumptions are recorded where a hypothetical rule first mutates authoritative state, with distinct sources for sight, noise radius, alert policy, attack policy and knock-out policy, independent of callbacks; the constructed no-handler position-to-victory case (native 97 after a charge) becomes a test through snapshot / restore and replay. Until then `observe.script.tainted` covers script-side sources only, and `docs/harness.md` says so. |
| 2 | Asset-derived tables in reachable history | **Open, maintainer's decision.** Unchanged from review 8 row 8: the purge is a destructive rewrite of the public branch; the maintainer decides, and until then the history is non-compliant with ADR-0003 and the disposition says so rather than "Done". |
| 3 | Ordinary call result reader (`0x0a`) keeps the lexical validation bypass | **Accepted, open (core batch 2).** `Call` is fused with its destination the way `Native` was; `callee.has_result` validated; direct, divergent and loop-entry jumps into the former reader refused, with hostile translated and restored programs as tests. |
| 4 | Perception can consume the whole budget every tick and starve the later phases | **Accepted, open (core batch 2).** A persisted global phase cursor plus the inner cursors, or deterministic per-phase quotas; a maximum-size hostile snapshot test proving bounded progress of every phase. |
| 5 | Movement, animation and action-change scans are outside the budget | **Accepted, open (core batch 2).** A deterministic spatial index for obstacles, movement / animation / action scans under persisted cursors and the shared budget, maximum-snapshot regressions. |
| 6 | Restart drops the recorded starting money; HUD money is unsigned | **Done.** The session's reset descriptor now carries the starting money and both restart paths (pause menu, lost page) reset with it; `HudState.money` is an `i32` like the profile's and the VM's. A recording ends at any reset, so a replay never spans a restart; the restart itself reuses the descriptor. Test: reset with a starting money, record, restart from the pause menu, the money stays the session's. |
| 7 | New documents lack provenance; quoted wording in the combat report | **Done for `movies.md` and `combat-measurements.md`; hub document pending.** `movies.md` has a provenance section (build hash, method, public-documentation citations, status per claim); the two quoted phrases in the combat report are paraphrased. `sherwood-hub.md` gets its build hash, the list of tests that depend on it and the probe scripts' status once the implementer of the hub loader (in progress) lands, so the two do not collide. |
| 8 | Format envelope bypassable (`unwrap_or(1)`, settings rewritten to 1) | **Done.** Both documents require `format` to be exactly the integer 1 before anything else is read; missing, string and future versions are ignored with a log line. Tests: profiles without a version, with a string version, with version 2; settings without a version. |
| 9 | Option bars misuse `enabled` / `selected`; select-player `hovered` is the selection | **Done.** Bars are `enabled` (all clickable) with `selected` = on; select player's `hovered` is the pointer's element in the complete item array (buttons, then rows). Stored-only option controls remain as answered in review 8 row 10 (documented, to be wired with their subsystems). |
| 10 | Stale documentation; `debug.vm` outcome forcing in tests | **Partly done.** The harness example shows protocol 6 / ruleset 12; the roadmap no longer lists `;` as missing and qualifies the options line. The obsolete duplicate section of `stealth-and-combat.md` is removed with the combat batch (its owner is editing the file now). The won / lost flow tests keep the documented `debug.vm` shortcut until a mission can be won or lost through play: the combat batch in progress adds the canonical loss (the hero's death) and the won flow follows a winnable mission; the exception is named in `docs/harness.md` and `AGENTS.md` allows no other use. |

## Review-8 closure, as re-assessed

Rows 1 and 4 of the review-8 disposition are reopened by findings 1, 4 and 5 above (script-side sources are
closed, engine-side sources and phase fairness are not); row 6 by finding 3 (the native half is closed); row 5
by finding 6 (closed here); row 7 by finding 8 (closed here); row 9 by finding 9 (closed here).
