# Codex spec re-review 23: docs/original/spec-campaign-camp-saves.md (campaign, revision 3), 2026-09-13

Spec reviewer role (ADR-0009), with access to `re/`. Answered in the spec's next revision.

Reviewed revision 3 at commit `2cb9c2a`, blob `04e00f567bd79c0a0968ee82a47eef9907365db9`, against all 29 findings. Remaining findings:

1. **Blocker — CAMP-002; 0x005E1B70, 0x005F8030, 0x00643F50, 0x00643F72.** The warning-only signature policy is wrong. A mismatch invokes the fatal diagnostic path, which normally terminates the process. Consequently, **“a writer may emit sixteen zero bytes—the original will load such a file”** is false. This also corrects review 19’s premise. Withdraw zero-tag export compatibility; specify a policy-compliant way to supply valid tags or exclude original-compatible writing from clearance. Permissive OpenSherwood import is a separate deviation.

2. **High — CAMP-063/065/263, B8/B36, §8.8; 0x00452E70, 0x00452D20, 0x00455A70, 0x005F8030.** The same diagnostic distinction affects two obligatory candidates, equal location/priority, and a zero blazon divisor: these invoke the fatal path. Describing the subsequent instructions as normal continuation is incorrect. Logging and continuing may be OpenSherwood policy, but must be recorded as a departure. Correct the acceptance expectations accordingly.

3. **High — CAMP-327, T3; 0x005806C0.** The row **“11, 12 | none”** is wrong. Workshop kind **11 selects identity 2**; only kind 12 has no supervisor. Update T3, the claim and its scope statements. The remaining supervisor identities match.

4. **High — CAMP-333, T3, native 256; 0x0057BA60, 0x005806C0.** Native 256 compares the profile’s **first string, `sprite`**; the supervisor test compares its **second string, `sequence`**. Both retail hero records have different first strings but the same second string, which matches the supervisor requirement. Thus **both appearances qualify**. Replace **“one designer string and so one of those two records”** and remove this caveat. Specify the two file fields explicitly rather than treating campaign identity as the comparison itself.

5. **High — CAMP-047/048; 0x00566570, 0x0056B8D0.** Missing-file/chunk behavior remains overstated. The team limit is initialized to **0**. Failure to open the mission does not assign 5; simply encountering no `SCOT` chunk also leaves the initialized value. The assignments of 5 belong to particular failed chunk-validation paths. Distinguish missing files, absent chunks and rejected chunks, with separate acceptance cases.

6. **High — CAMP-203/204/260; 0x00451B70, 0x00455F00, 0x004564E0.** Two observable reduction effects are missing. Clearing an existing `deferred` list also resets its members’ ages. On forced reduction of the secondary list, step d is **not** rerun, and the explicit reset of unchosen source-slot ages belongs to the primary-list branch. Correct the shared forced-path description and test with pre-existing deferred entries.

7. **High — CAMP-211, B13; 0x00452C90.** B13 contradicts the corrected rule. From three candidates containing two recent missions, removing the newest leaves two, so processing **continues**: the other recent candidate is removed and the third remains. The stop condition is fewer than two. Store test history oldest-first and distinguish its storage order from visitation order.

8. **High — CAMP-263, B35; 0x00455A70.** The conversion uses the **selected node and team size**, not an unspecified “gang_or_team_size.” An exactly met requirement yields zero; only an exceeded requirement underflows. Nor does an underflow necessarily produce a large final result—the team-derived bound can win. For B35 with team size 8, divisor 3, requirement 2 and blazons 5, the original’s low-word result is **6**. Supply complete inputs and precise expectations.

9. **High — CAMP-250, B24; 0x004E3260, 0x0049F510, 0x004A05A0.** The alleged **“item list” / “loot pass”** is misidentified. This pass visits player characters, tests the saved byte currently called `unknown_a`, and invokes character-state changes that clear it. It does not establish collection of level items. Correct the participating objects and specify the character effects—or exclude them. B24 cannot assert an unsupported loot operation.

10. **High — CAMP-071/290/292, B53/B54, §8.7; 0x004524B0, 0x0055BF00, 0x004565C0, 0x00456670.** An empty recovery list suppresses the **coin flip**, not all randomness: the new-character branch contains another draw. A non-empty list likewise does not imply only one draw on the new-character branch. Allocate and specify that draw before claiming complete RNG consumption. Also, `labels` is the generated-name history: an unused generated name is appended; collisions retry up to ten attempts, followed by the fixed fallback. Correct its “purpose unknown” description and the retry contract without reproducing fallback text. Recovery’s unspecified experience/health calculation still prevents clearing the complete recruit mechanism.

11. **High — CAMP-323/326/328, §§3.11/11; 0x005810C0, 0x0055BB00, 0x00580430, 0x00580520, 0x00580940, 0x00580DC0.** The supposedly implementable workshop remainder is incomplete. Kind 9’s additional capability is **id 1**. The exceptional stock-count contribution needs an identified element/state predicate; **“certain actors”** is insufficient and does not follow from that branch. Assignment capture visits the registered workshop zone’s members, not an unrestricted scan of every camp player character. Its eligibility rules, absent-zone behavior and invocation point remain missing. Placement also skips absent actors and records their workshop association. Settle these results or narrow the clearance.

12. **High — CAMP-270, B29/B30; 0x00456DB0.** The special progress cases still omit the exact interface tokens. They are **`HI` → 100** and **`DD` → 95**, with the 100-percent case taking precedence. “The last story/defend mission” does not define behavior for reordered or modified data. Record the codes individually with file provenance and test both being won.

13. **Blocker — snapshot contract, CAMP-071/091/320/326; 0x00452F20, 0x004565C0, 0x00456670, 0x00580220, 0x00580330.** Section 8 both includes the capacity flag and excludes `full`, although they are the same field. It omits `labels`, which affects future names, and `spare_refs`, which the offered round-trip contract preserves. Workshop zone associations and ordered native-200 registrations affect future behavior; omission from the original save does not make them non-authoritative. Rerunning a script load path is not yet a defined, side-effect-safe snapshot reconstruction procedure. Specify ownership, preservation/reconstruction and restore-equivalence tests, including the selected profile’s behavior-relevant difficulty.

14. **High — CAMP-271/272, §7 oracle procedure, §8; 0x0050E7B0, 0x0050E800, 0x00511340, 0x0055D370.** Wall-clock accumulation into hashed counter 6 has no OpenSherwood determinism decision. Define the authoritative time source, checkpoint truncation and restoration of an active interval. Test repeated subsecond saves. The oracle procedure also checks the wrong immediate output: an ordinary save updates the saved campaign counter, but does not itself invoke the profile-summary updater. Observe counter 6 in the live save block, and test profile updating separately.

15. **Blocker — CAMP-013/101/200/251/310/313, native 173, §§10–12; 0x004E3260, 0x0055D370, 0x0051D480, 0x0055BC90, 0x00453E60, 0x00579210.** **“Everything else in sections 2 to 8 is offered for clearance”** still includes essential unknown behavior: the 70-point qualification, profile percentage expression, persistent-character initialization, native-165 actor effects, native-173 transitions, and spawn assignment. Native 170’s membership test does not specify spawn matching. The spawn routine additionally contains camp-slot reuse, randomization, fallback matching and a camp team-clear operation. Amend these contracts or add explicit exclusions and assumptions. Also remove **“the camp’s item production [is] implementable”**, which contradicts the retained output exclusion.

16. **Medium — CAMP-111, §2.6.5; 0x0055D1A0.** **“The player’s name … at most 31”** is not a serializer constraint established here. The reader takes a u32 length and allocates storage accordingly; the comparison with 31 concerns string capacity management. Remove the asserted wire limit or establish a separate input/UI limit with provenance.

17. **Blocker — expression filter, CAMP-350/351, §§3.13/5; 0x0048F280, 0x004A5C30, 0x0046FFE0, 0x004B1EB0, 0x005788A0.** The complete palette remains reproduced across prose and constants rows, including **“RGB (165, 255, 82),” “RGB (115, 40, 203),”** and **“RGB (255, 255, 0) and RGB (0, 120, 255).”** The clarification explicitly retains the prohibition on colour lists; splitting one palette into individual facts does not clear it. Remove the aggregate palette or obtain an explicit policy amendment. Native 24’s accepted codes and preservation of previous overrides can be specified independently.

18. **Medium — expression filter, CAMP-022/070/134; 0x0054CC40, 0x0051D5D0, 0x00511340.** Unnecessary recovered organization remains: **“a two-field loop that runs twice,” “the four bytes of one stack location,”** and **“Save and load are one routine with a direction flag.”** Retain the wire order, discarded-byte framing and observable load/write effects; remove these implementation details. The stack-origin assertion also exceeds what the required four-byte framing establishes.

19. **Medium — acceptance contract, §§7/12; addresses above.** Beyond the incorrect cases identified above, acceptance coverage still lacks entry-by-entry T1/T2/T3 checks, both hero appearances, failed-attempt state persistence, deferred-list clearing, and canonical snapshot continuation. B22 needs the aging preconditions; B43 must restrict its “no output” assertion to kinds 0–11. Define supported archive versions and old-version defaults explicitly, and distinguish campaign-prefix support from complete save interoperability—the level/actor payload remains unspecified.

**Can be cleared now:** the corrected character-record framing, four node-reference order, town-code representation, conditional backup/unconditional live-block framing, and version-46/47 assignment framing. Local parsing confirms the 98-byte character record, campaign-block boundaries, 30,441-byte CPF and 430-byte profile archive. T1’s ten mappings, its capability substitutions, and T2’s nine mappings pass both factual review and the clarified expression filter. The corrected victory gating, temporary placement quantity, experience-slot/health destinations, hostile survival counters, delta-only statistics propagation and native-24 override preservation are supported within the limitations above. The identity/exposure record and separately pending publication approval are acceptable. The heuristic asset check passes; it does not clear findings 17–18.

**Verdict: fix-then-clear.**