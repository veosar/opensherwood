# Disposition of Codex review 11 (2026-09-06)

Review: `2026-09-06-codex-review-11.md` (commits `1d4d428..bce71e5`). Verdict there: redesign, on the
history gate, the rollback of ordinary callbacks, a lost stimulus, the item model against the measurements,
fight reciprocity as an invariant, and test depth.

| # | Finding | Disposition |
|---|---------|-------------|
| 1 | History gate; the `H01_ITEMS` table in a test; quoted wording in two documents | **Table and wording: done in the ruleset-17 batch and here; history: open, maintainer's decision.** The item table is replaced by structural assertions read from the player's file at test time (the ruleset-17 implementer, who owns the file); the three quoted phrases of `h01-measurements-2.md` and the fragment in `rhm.md` are paraphrased. The rewrite of the public history stays the maintainer's call (review 8 row 8). |
| 2 | An ordinary callback (`Hourglass`) that overflows the stack keeps its effects; the same tick's victory test reads them untainted | **Accepted, open (core batch 4).** Every callback runs in the transaction (class and mission variables, native effects, entities, selection, camera, queues, sequences) and an `Aborted` outcome rolls it back; regression: `Hourglass` sets a variable, recurses to the overflow, `CheckVictoryCondition` in the same tick and later ticks, through snapshot / restore, with native side effects. |
| 3 | A one-tick noise stimulus is lost when the charge's search is unpaid and the run ends | **Accepted, open (core batch 4).** A pending transition (position, channel) is state (snapshotted, hashed) consumed on the retry, or the work is reserved before the stimulus is consumed; test: the run completes right after the underfunded transition, with snapshot and ReplayV1 continuation. |
| 4 | The item model contradicts the measurements (12 px base hit test, 24 px take, no stoop, first frame only, kind 8 unknown) | **In progress (ruleset 17, the batch running when this review arrived).** Order-bound take at 0..8 px with a hashed stoop phase and timer, sprite-area hit test, the pouch as a known kind, the sparkle frames; mid-stoop snapshot and a ReplayV1 through a pickup. |
| 5 | Reciprocity validated only when the opponent is also fighting; a punching second attacker bypasses the wait | **Accepted, open (core batch 4).** Every living, active fighter's opponent must fight back with the reciprocal id (hostile snapshots with a patrolling or returning opponent refused); the engaged-victim branch stops every second attacker, behind and punch included, until an explicit multi-attacker model is measured. |
| 6 | Replay and end-to-end claims overstated: in-core event replay, snapshot-only fight and item tests, any-successor assertion, colour-only mini-map test | **Partly done here, the rest with batches 17 and 4.** `test_win.py` asserts the documented successor by name; the mini-map test checks the hero's oval, a pick-up cross, the camera corner and a far soldier at their computed positions; ReplayV1 cases through a pickup (ruleset 17) and through a reciprocal fight (core batch 4) follow. |
| 7 | Documents describe incompatible states (win-path row 3, `rhm.md`, roadmap, an engine comment) | **Done.** The walkthrough row says what ruleset 15 did and what ruleset 17 aligns; the roadmap no longer lists mini-map markers as missing; the engine comment describes the successor and the lost page; `rhm.md`'s item paragraph is corrected by the ruleset-17 batch. |

Review-10 rows reopened here: 3 (as row 2), 4 (as row 3), 7 (as row 5), 9 (as row 6), 10 (as row 1).
