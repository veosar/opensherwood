# Disposition of Codex review 11 (2026-09-06)

Review: `2026-09-06-codex-review-11.md` (commits `1d4d428..bce71e5`). Verdict there: redesign, on the
history gate, the rollback of ordinary callbacks, a lost stimulus, the item model against the measurements,
fight reciprocity as an invariant, and test depth.

| # | Finding | Disposition |
|---|---------|-------------|
| 1 | History gate; the `H01_ITEMS` table in a test; quoted wording in two documents | **Table and wording: done in the ruleset-17 batch and here; history: open, maintainer's decision.** The item table is replaced by structural assertions read from the player's file at test time (the ruleset-17 implementer, who owns the file); the three quoted phrases of `h01-measurements-2.md` and the fragment in `rhm.md` are paraphrased. The rewrite of the public history stays the maintainer's call (review 8 row 8). |
| 2 | An ordinary callback (`Hourglass`) that overflows the stack keeps its effects; the same tick's victory test reads them untainted | **Done (ruleset 18).** Every callback runs in the transaction and an aborted one (trap, fault, overflow) is rolled back, native effects included; test `an_overflowing_hourglass_is_rolled_back_and_never_wins`. The design was: Every callback runs in the transaction (class and mission variables, native effects, entities, selection, camera, queues, sequences) and an `Aborted` outcome rolls it back; regression: `Hourglass` sets a variable, recurses to the overflow, `CheckVictoryCondition` in the same tick and later ticks, through snapshot / restore, with native side effects. |
| 3 | A one-tick noise stimulus is lost when the charge's search is unpaid and the run ends | **Done (ruleset 18).** `Entity::pending_stimulus` (hashed, validated) is consumed by the retry; test `a_one_tick_stimulus_survives_an_unpaid_transition` through a restored snapshot (a ReplayV1 continuation is not expressible over the RPC: the budget is not controllable there). The design was: A pending transition (position, channel) is state (snapshotted, hashed) consumed on the retry, or the work is reserved before the stimulus is consumed; test: the run completes right after the underfunded transition, with snapshot and ReplayV1 continuation. |
| 4 | The item model contradicts the measurements (12 px base hit test, 24 px take, no stoop, first frame only, kind 8 unknown) | **Done (rulesets 17 and 18).** Sprite-area hit test, arrival within 8 px, the hashed stoop playing the pick-up action, the pouch as a known kind (drawn from the money-bag bank, its effect still tainted), the 16-frame sparkle cycled on the animation clock; mid-stoop snapshot and a ReplayV1 through a pickup. The plan was: Order-bound take at 0..8 px with a hashed stoop phase and timer, sprite-area hit test, the pouch as a known kind, the sparkle frames; mid-stoop snapshot and a ReplayV1 through a pickup. |
| 5 | Reciprocity validated only when the opponent is also fighting; a punching second attacker bypasses the wait | **Done (ruleset 18).** Validation requires the reciprocal fight of every living fighter (hostile patrolling / returning opponents refused); every second attacker waits, behind and punch included; test `a_second_attacker_from_behind_waits_while_the_victim_is_engaged`. The design was: Every living, active fighter's opponent must fight back with the reciprocal id (hostile snapshots with a patrolling or returning opponent refused); the engaged-victim branch stops every second attacker, behind and punch included, until an explicit multi-attacker model is measured. |
| 6 | Replay and end-to-end claims overstated: in-core event replay, snapshot-only fight and item tests, any-successor assertion, colour-only mini-map test | **Done, one small item open.** `test_win.py` asserts the successor is another mission (its exact name follows once the campaign graph's reading is documented); the mini-map test checks the hero's oval, a pick-up cross, the camera corner and a far soldier at their computed positions; ReplayV1 cases through a pickup (`test_a_pickup_replays_through_the_stoop`) and through a reciprocal fight (`test_a_reciprocal_fight_replays_with_equal_hashes`) are in. |
| 7 | Documents describe incompatible states (win-path row 3, `rhm.md`, roadmap, an engine comment) | **Done.** The walkthrough row says what ruleset 15 did and what ruleset 17 aligns; the roadmap no longer lists mini-map markers as missing; the engine comment describes the successor and the lost page; `rhm.md`'s item paragraph is corrected by the ruleset-17 batch. |

Review-10 rows reopened here: 3 (as row 2), 4 (as row 3), 7 (as row 5), 9 (as row 6), 10 (as row 1).
