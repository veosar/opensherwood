# Disposition of Codex review 7 (strict-mode taint, money seeding, action changes, AI budget, profile indices, native arity, AI invariants)

Review: `2026-09-03-codex-review-7.md`, verdict redesign. Rows 1, 3, 4, 5, 6, 7 and 9 answered by the core
implementer (`opensherwood-core`, `opensherwood-script`, `mission.rs`, the data tests); rows 2, 8, 10 and 11 are the
lead's. Versions after the core rows: ruleset 10, hash schema 12, snapshot schema 13 (2026-09-05);
`harness/fixtures/synthetic_corridor.json` regenerated and `GOLDEN_CORRIDOR_TOTAL` updated deliberately (the
corridor has no script; only the schema prefix and the new `ai_cursor` byte moved). Validation: `cargo fmt`,
`cargo clippy -p opensherwood-core -p opensherwood-script --all-targets -- -D warnings`, `cargo test` (core 87,
script 7, app 23), `python -m pytest harness/tests` with `OPENSHERWOOD_GAME_DIR` set.

| # | Finding | Disposition |
|---|---|---|
| 1 | Strict execution fabricates authoritative mission predicates from stub values | **Done (taint model, ADR-0008 "Hypotheses and taint").** `VmState::assumptions: BTreeSet<vm::Assumption>` (snapshotted, hashed under `scripts`, validated: a `StubResult` must name a stub) records every dependency of script-visible state on a hypothesis: `StubResult(id)` when `GetNativeResult` consumes a policy-valued or zero-valued stub's result, or when one of `natives::NEVER_WIN_STUBS` (70 / 178 / 182 / 213 / 222 / 223 / 232 / 234) is called; `Perception` / `KnockOut` when an alert or knock-out action id is delivered to `ActionChange`, native 90 reports a knocked-out actor or 128 refuses one; `ProfileStats` when a blow consults the knock-out resistance (`p4`; `p0` is not read by anything yet); `TickRate` when a native-56 wait runs or `Hourglass` reads its time; `CampaignGraph` through `World::record_assumption` (the lead wires the app's successor rule); `LenientAssets` through `MissionSpec::assumptions` (row 6). `mission_won` / `mission_lost` stay recorded; `ScriptObservation` exposes `tainted` and `assumptions` (`observe.script`; `debug.vm` is the lead's), documented as not authoritative when tainted. Unknown ids still trap. Strict run of all 37 loadable missions (seed 1, 300 ticks): no trap, no arity mismatch; the taint of every mission is listed in `docs/formats/scb.md` "Engine notes"; H01 carries `stub_result 235` and `tick_rate` from its first tick after the briefing. Tests: `stub_results_and_hypotheses_taint_the_outcome` (consumed versus ignored stub results, the never-win call, the wait and the Hourglass time, a clean script, hashing, JSON round trip, validation of `StubResult(3)` / `StubResult(999)`, `record_assumption` without a VM), `state_natives_read_the_stealth_layer` (90 / 128 record `KnockOut`), the data tests `test_first_mission_briefing_sequence_then_camera_on_the_hero` (clean after the briefing, `[{"stub_result": 235}, "tick_rate"]` after 600 ticks), `test_every_mission_script_translates_and_runs_300_ticks_strictly` (`tainted` agrees with the set everywhere), `test_running_past_a_soldier_is_noticed_then_the_alarm` (`perception`), `test_knock_out_from_behind_puts_the_soldier_out_of_action` (`knock_out`, `profile_stats`). |
| 3 | Session installation overwrites the money set by `Initialize` | **Done in core; the lead removes the overwrite.** `MissionSpec::starting_money: i32` (`DEFAULT_STARTING_MONEY` = 100, serde default) is applied to `VmState::money` by `attach_script` before `Initialize` runs, so H10's `n237(100000)` wins and nothing in core touches it afterwards. The app change (engine.rs, lead-owned): in `reset`'s mission arm set `spec.starting_money = i32::try_from(self.profile().money).unwrap_or(0)` next to `spec.lenient_natives`, and delete the `if let Some(vm) = world.vm.as_mut() { vm.money = ... }` block of `Session::install` (the HUD keeps reading `vm.money`). Tests: core `money_natives_share_one_hashed_integer` (unchanged: the script's value survives), data `test_starting_money_is_seeded_before_initialize` (H10 = 100000 right after `reset` and after 5 ticks; H01 = 100 after the briefing and 5 ticks) — green once the engine.rs change is in (verified against a local copy of that change, then reverted). |
| 4 | `ActionChange` is lost when the tick's budget is exhausted | **Done (exactly-once queue).** `VmState::pending_action_changes: Vec<ActionChange>` (snapshotted, hashed under `scheduler`, validated: class in range, bounded by `MAX_QUEUE` with a drop counter). `World::simulate` queues every change it detects and calls `vm_deliver_action_changes`, which removes a change only after `CallOutcome::Returned`, after a trap / fault (`Aborted`: the handler ran and would fail the same way again) or when the class has no `ActionChange` (confirmed absent); a budget cut (`CallOutcome::Exhausted`, the new outcome of a budget abort) leaves it at the front and `vm_tick` delivers the leftovers after the messages and before `Hourglass`. Test: `action_changes_survive_an_exhausted_tick_and_are_delivered_once` (a spinning `Hourglass` exhausts tick 0 before the guard notices the hero: the change waits in the queue with the handler untouched; the queue is hashed, JSON round-trips and is validated; tick 1 delivers it exactly once in the original and the restored world with equal hashes; the alarm's change follows without a repeat; a class without a handler drops its changes). |
| 5 | The AI work bound is bypassable | **Done (one AI budget with a round-robin cursor).** `ai::AI_WORK_PER_TICK` (2^24) is granted at the start of `ai_tick` and pays for everything the layer does: perception pre-indexes the perceivable player characters once (one unit per entity), inspects the entities from `World::ai_cursor` on (one unit each, whether or not they perceive) and charges every soldier / player character pair it tests; a scan the budget cuts short leaves the cursor on the entity not finished (authoritative: snapshot, `validate`, the `world` hash) and the next tick resumes there; every path search the layer issues (the alert run, the re-plan, the return, the attack approach) draws from the same budget through `plan_path_with`, capped per search at `ORDER_SEARCH_WORK`. Tests: `perception_charges_every_inspected_entity_with_no_player_to_perceive` (`MAX_ENTITIES` = 65536 soldiers and no player: exactly 2 x 65536 units, well within the budget, cursor 0), `perception_resumes_from_its_cursor_when_the_budget_runs_out` (exact unit accounting per pair, the cursor after an exhausted scan, its hash / snapshot / validation, the wrap-around, a budget too small for the pre-index pass, equal hashes after a restore), `mass_alerts_share_one_navigation_budget` (600 soldiers hear the running hero and all want a path on the same tick: within the bound with the real budget; with a small budget only as many as it pays for are planned, the rest re-plan on the following ticks, deterministic across a snapshot). |
| 6 | Strict loading accepts out-of-range profile indices | **Done.** `mission::check_profile_indices` names every `BORG` / `OILE` / `TOTO` record whose index lies outside its table; `load` refuses the mission on the first (strict) or logs each (lenient), and `build_spec_checked` refuses, unless lenient, any actor left on a default sprite (index, unavailable sprite or missing table), in which case the spec carries `Assumption::LenientAssets`, seeded into the VM at load. Test: `strict_loading_refuses_out_of_range_profile_indices_and_lenient_records_the_fallback` (the index-99 record is named, strict `build_spec_checked` refuses it, lenient records the assumption and the default sprite, a clean mission passes strictly with no assumption and the default starting money, an unavailable sprite behaves the same). All 37 retail missions still load strictly (`test_every_mission_file_loads`). |
| 7 | Native arity is not part of the trust boundary | **Done (one signature table).** `natives::NATIVE_SIGNATURES` (`id -> arity, has_result`) covers exactly the implemented and stub ids, from the arity column of the spec rows (130 and 137 got arity-only rows in `scb.md`; the corpus has one arity per id, reads a result always or never, and every `0x0d` directly follows its `0x0c`: 20079 reads over 39 files). The translator refuses a call site with another push count and a result read after a native without one (or without a call before it); `Program::validate` repeats both checks; the dispatcher checks the count again and traps a mismatch (`counters.arity_mismatches`, `faulted`), so no required argument defaults (`natives.rs`: the accessor's fallback is unreachable; the two-argument message forms are the one documented absence). Tests: `signature_table_covers_the_known_natives`, `native_arity_is_validated_and_never_defaults` (n237 with 0 / 2 arguments refused by `validate`, the result of n237 refused, n236 accepted, a read without a call refused, unknown ids unconstrained; dispatch traps and leaves the money untouched; injected past validation the callback aborts quiescently), script `invalid_scripts_are_refused` (the three translator errors). |
| 9 | Invalid semantic combinations of AI state are accepted | **Done.** `ai::ActorStatus::of` is the one state function natives 85 / 87 / 90 / 128 / 240 read (dead = the `Dead` state, which `World::validate` requires to coincide with `alive == false`; present = `active`; knocked out; out of action; can act). `validate` also requires a timer exactly in the timed states (`AiState::timed`), alert states on enemy soldiers only, `Punching` on player characters only, an alert origin while returning, no memory while patrolling, and an attack order from a player character to an existing enemy soldier other than itself. Tests: `actor_status_is_one_reading`, `stealth_state_survives_snapshots_and_is_validated` (ten rejections, the consistent dead form accepted), `state_natives_read_the_stealth_layer` (dead is removed, dead, out of action and unable to act at once). |
| 2 | Hypotheses drive script state | **Done (taint), measurement started.** Every outcome that rests on a hypothesis is recorded as an assumption (row 1) and `observe.script.tainted` says so; the analyst measured the original on 2026-09-05 (`docs/original/stealth-and-combat.md` section 8: walk 85 px/s, run 101, sneak 18, a ~64 Hz animation clock, detection of a running hero from 330 px with an immediate charge) and the constants are being replaced by those values, each replacement removing its assumption. |
| 8 | Bulk asset-derived tables | **Checkout clean, history not purged (review 8 finding 8).** The per-block animation and per-record profile tables were reduced to id roles, reading rules, a presence matrix and value intervals (`5a5f956`, `1e809ae`); the full tables live only in the ignored analyst workspace. The pushed history keeps them (maintainer's decision, with the earlier items). |
| 10 | HUD keys leak into the held-key set | **Done.** HUD figure clicks emit a balanced press and release; a release arriving in a later tick is swallowed (`6d82707`). |
| 11 | Status report stale | **Done.** Counts, scenario and verification claims regenerated from the code and tests. |

Review 8 (`2026-09-05-codex-review-8-disposition.md`) reopens rows 1, 3, 4, 5, 7 and 20: the taint set is not dependency-closed, the action-change queue retries partially run handlers, the AI budget does not cover every phase, campaign money was mission-local, and "every hypothesis-tainted outcome" was overstated. The remaining work is listed there.
