# Codex spec re-review 29: docs/original/spec-campaign-camp-saves.md (revision 4), 2026-09-18

Spec reviewer role (ADR-0009), with access to `re/`. Answered in the spec's next revision.

Reviewed [revision 4](C:/Users/przem/source/repos/opensherwood/docs/original/spec-campaign-camp-saves.md) at `7406a11`, blob `166db83c3259999363cc0543251b7d1ec14b3e1b`, against all 19 findings, the decompilation and campaign notes. Addresses below are virtual addresses. The identity/exposure block is acceptable; publication approval remains separately pending. The heuristic asset check passes.

1. **Blocker — CAMP-271/272; §8.4, B61; 0x0050E7B0, 0x0050E800, 0x00511340.** The new time contract is not mathematically complete. After 22 frames, removing one whole second leaves **2/64 second**, which cannot be represented as an integer number of frames. “Leaves the remainder in the residue” needs an exact unit and representation. Also, restoring `interval_start_frame` and then starting the interval at the restoring frame loses elapsed time since the preceding checkpoint. Specify exact fractional units, active/inactive interval state, initialization and preservation of uncheckpointed elapsed time. B61’s quarter-second interval is not an integer number of logic frames; use integral frame counts and assert the exact remainder.

2. **Blocker — CAMP-091/300/320; §§8.3, 8.6, 12; 0x0050F710, 0x00511340, 0x00580D20.** Campaign-prefix import does not establish equivalent camp restoration. Re-entering with a won `previous` can apply production, training or healing **again** to an already updated live campaign block. The assertion **“which is the same path the original itself takes”** is unsupported: original loading restores the level payload and distinguishes loading from normal entry. Define precisely which state is retained, discarded and recomputed, and how repeated entry effects are prevented—or exclude playable camp restoration and offer extraction only. This also depends on the unresolved camp ordering and spawn contracts.

3. **High — snapshot contract, CAMP-071/091/320/326; B60; 0x00452F20, 0x004565C0, 0x00580220, 0x00580330.** B60 is a serialization comparison, not the requested canonical continuation test. Saving can itself change time and slot state, so equality with a snapshot taken before saving also needs a defined checkpoint boundary. Require identical subsequent canonical inputs on uninterrupted and restored runs, comparing hashes, RNG positions, generated names, offers and workshop effects. Keep this separate from the deliberately lossy original-save import test.

4. **Blocker — CAMP-250/253; §§3.5, 10–12; 0x004E3260, 0x0049F510, 0x004A05A0, 0x00464630.** The player-character flag and health corrections are supported, but the complete revive pass remains uncleared. Its unidentified effects include the final operation, which schedules further actor activity; section 11 excludes none of this, while section 12 explicitly includes revive bookkeeping. Add an exclusion or settle all resulting state changes and ordering. The expression **“one taking the arguments (0, 0, 1) and one taking (19, 0, 255)”** also fails the expression filter: these are untranslated internal call arguments without established functional meanings. Remove them from the public specification.

5. **Blocker — expression filter, CAMP-350/351; §3.13, B55; 0x005788A0.** Removing RGB numbers did not fully remove the colour list. Remaining text includes **“111 set light green, 200/201/202 and 222 red, 300/301/302 and 333 cyan”** and **“the colours stay light green.”** These retain palette assignments despite the explicit exclusion. Remove the colour assignments; test override preservation using independently chosen synthetic colours. Accepted codes and the human-family restriction can remain.

6. **Medium — expression filter, CAMP-134; §3.10; 0x00511340.** **“Loading is the same routine with the direction reversed”** still prescribes recovered function organization—the subject of review 23 finding 18. Replace it with the observable load order and effects. CAMP-134’s “same sequence … in opposite directions” should likewise distinguish shared archive order from write-only and load-only effects.

7. **High — CAMP-002, `OriginalReadableWrites`; §8.2; 0x005E1B70, 0x005F8030, 0x00643F50.** The donor idea is policy-compatible in principle, but its compatibility guarantee is incomplete. A permissively parsed tag is not thereby a valid original tag; the proposed scan can encounter OpenSherwood markers, conflicting donors or incomplete archives. **“A stock installation carries every tag”** is not established by mutable local fixtures. Specify donor eligibility, grammar-based extraction, target-build association, conflict handling and exclusion of OpenSherwood-generated markers. Missing or untrusted donors must prevent an original-compatible export claim. Validate donor-backed campaign/profile exports with the original; valid tags alone cannot clear complete saves whose level payload remains unspecified.

8. **High — CAMP-063/065/263; §§2.2, 3.3.1, 3.6; 0x00452E70, 0x00452D20, 0x00455A70.** Correct claim rows coexist with the old fatal-error behavior. The reduction table still says **“diagnosed and then ignored, leaving both”** and **“diagnosed and both retained.”** The zero-divisor descriptions still describe returning after the fatal diagnostic. State termination as the original’s normal behavior throughout, and label the retained-candidate/zero-result behavior exclusively as OpenSherwood’s deviation.

9. **High — CAMP-260/264; step d, B64; 0x00455F00, 0x004564E0.** **“Its first act is to clear `deferred`”** is overbroad. Clearing and resetting former members’ ages occur only when the working candidate list has **more than one entry**. With zero or one entry, the existing deferred list and its ages remain unchanged. Correct the reduction table and give B64 explicit candidate-count preconditions, with separate zero-, one- and multiple-candidate cases. The revised primary/secondary forced-path distinction is supported.

10. **Medium — CAMP-047; §2.2; 0x00566570, 0x0056B8D0.** A failed mission-file open leaves `team_limit` at zero, but **“nothing is reported”** is false: the failure branch issues a non-fatal diagnostic. Distinguish that from a successful chunk walk finding no `SCOT`, which leaves zero without that report. Retain the separately established validation-failure value of five.

11. **High — CAMP-071/290/295; §§2.1, 2.3, 3.8, B53/B54; 0x004524B0, 0x0055BF00, 0x0055BDF0, 0x004565C0, 0x00456670.** The corrected recruit/name prose has not replaced contradictory definitions and tests. B53 still expects no recruit-stream draw; B54 omits the additional new-character draw. The `labels` row still says **“purpose unknown”**, and §2.3/CAMP-071 still leave the retry exit unresolved. Synchronize these with the established history/retry rule. Specify the coin polarity: **even selects new-character creation; odd selects recovery**. State draw counts per stream so the one/two recruit draws are not mistaken for including name generation.

12. **High — CAMP-326/328/331a; §§3.11, 8.3, 11; 0x00456A70, 0x005803C0, 0x00580520, 0x00580B70, 0x00580DC0.** Two workshop lifecycle details remain wrong or missing. Character placement visits **assignments in assignment order**, not registered work spots; work-spot order governs item placement. After each workshop’s capture, the campaign pass **clears its zone association and work-spot list**, an omitted authoritative state transition. Record both, and state explicitly that capture replaces assignments when a zone exists, including an empty zone. The capture invocation point remains unknown, so section 11 cannot clear its integration merely by excluding output arithmetic. The new stock predicate and kind-9 capability predicate themselves are supported.

13. **High — CAMP-301/303; §§3.9, 6, 8.3; 0x0050F710.** **“Submitted once, at camp start”** omits another submission in the same-level return/load path. It also conflicts with VM-113 in the expressly pinned `afdfaec` specification, which records both invocation contexts. Reconcile the occurrence conditions with that dependency and distinguish message submission from completion ordering. This matters because native 200 appends registrations.

14. **High — CAMP-100/101; §§7, 10–12; 0x0050F710, 0x0055D370.** The twelve exclusions omit **profile-summary update scheduling**. Excluding only the stored percentage expression does not settle when score, money, progress and cumulative profile playtime update. Contrary to an inference from the empty caller inventory, the exported game loop contains calls to the updater. Trace their behavioral triggers or explicitly exclude the complete updater integration. The ordinary-save oracle correction is supported.

15. **High — CAMP-093/124/125; §8.6, B62; 0x00452F20, 0x00580AB0, 0x0056EA70.** **“Accepts versions 28 and above”** claims support for unanalysed future formats. Bound support to the documented versions, such as 28–48, and reject others unless separately specified. Define how the two saved-game version words are validated when they disagree; the original uses the inner archive version to control decoding. Test each format boundary and loading older versions over already populated state.

16. **Medium — acceptance contract; CAMP-002/047/071/211/270/326/331a; §7; addresses above, especially 0x00452C90 and 0x00456DB0.** B14a remains impossible as written: five distinct candidates containing three recent entries leave **two**, not one. Other requested cases remain absent: signature mismatch, two obligatory candidates, separate missing/chunkless/rejected mission files, name collision retries and fallback history, both special progress codes won together, exceptional stock contributions, and absent/empty workshop zones. Give B63 concrete inputs and draw outcomes that actually cause a failed attempt. The added T1/T2/T3 coverage and corrected B22/B43 are useful, but do not close these gaps.

**Cleared implementation scope is limited to these components:**

- **Campaign graph and offers:** graph interpretation, accessibility, choice/aging and established outcome/blazon arithmetic. The complete offer reducer waits for findings 8–9 and corrected acceptance cases.
- **Save and profile grammar:** documented campaign-prefix and profile/configuration reading, including character framing and version-46/47 assignment framing. Future-version support, original-compatible writing and playable restoration wait.
- **Camp deployment:** T1 membership/capability checks, T2 mappings and corrected T3—including kind 11 and both hero appearances. Complete deployment, workshop integration and camp execution remain blocked by the stated exclusions and findings above.
- **Statistics:** counter identities, established arithmetic, map spared percentage, progress including `HI`/`DD` precedence, and delta-only propagation. Complete hostile qualification, revive effects, profile updating and authoritative time wait.

All twelve section-11 exclusions remain in force; add explicit boundaries for revive effects, capture scheduling and profile-update scheduling. The identity block does not clear those behaviors.

**Verdict: fix-then-clear.**