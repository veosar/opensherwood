# Status 2026-09-13: the decompilation-driven rebuild (ADR-0009)

Starting point: commit `73e91d4` (ruleset 18): every screen, all 39 missions loading, mission 1 won and lost
through play, melee, items, a stealth layer, all built from data observation and marked with assumptions where
the executable decides the rule (`docs/status/2026-09-02.md`).

## Done

- Governance: ADR-0009, the two-tier wall in AGENTS.md / legal.md, the `analyst-ghidra` skill, the spec
  template, the gate against decompiler output in `check_no_assets.py`, the Ghidra export scripts.
- Ghidra: the executable imported and auto-analysed (project in the ignored `re/`), inventory / strings /
  full decompilation exported for the analysts.

## Specifications (state on 2026-09-18)

| Spec | Revision | Reviews | Cleared so far |
|---|---|---|---|
| `spec-script-vm.md` | 5 (6 in revision) | 14, 17, 21, 25, 28 | instruction / calling / native-result protocols, messages, both-buffer snapshot, settled natives; the scheduler and sequences wait |
| `spec-navigation.md` | 3 (4 in revision) | 15, 20, 26 | static formats, availability predicate, request states, reconstruction direction, native corrections |
| `spec-ai-combat.md` | 3 (4 in revision) | 16, 22, 27 | layouts, stimuli, hearing corrections, difficulty, rail advancement, duel gate, stun / deflection boundaries |
| `spec-movement-animation-camera.md` | 3 | 18, 24, 30 (pending) | timer stepping and the displacement gate, mode-3 arithmetic, turning, element traversal, 49/50 completion |
| `spec-campaign-camp-saves.md` | 4 | 19, 23, 29 (pending) | character-record framing, node references, town code, block framing, interface tables T1 / T2 |

Each review round narrows the findings (23-29 in round one, 11-16 in round two, 7-13 in round three); the
verdicts are "fix-then-clear" with growing per-fact clearances. ADR-0010 fixes the logic frame.

## In progress

- Implementer batch 1 (ruleset 19): the VM interpreter core, the settled natives, messages and the single logic frame, from the cleared parts of the VM spec.
- Analyst specifications (in order): script VM and natives; navigation (path graph, layers, doors, lifts);
  AI, stealth and combat; movement, animation and camera; campaign, the camp and saves; HUD and menus.

## Next

- Implementer batches from each reviewed spec; the `Assumption` registry shrinks with each; mission 1's
  intended route and every objective; each campaign mission won through play with a recorded replay; the camp;
  the full campaign; movies (licence decision pending: `docs/formats/movies.md`).
