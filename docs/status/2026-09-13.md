# Status 2026-09-13: the decompilation-driven rebuild (ADR-0009)

Starting point: commit `73e91d4` (ruleset 18): every screen, all 39 missions loading, mission 1 won and lost
through play, melee, items, a stealth layer, all built from data observation and marked with assumptions where
the executable decides the rule (`docs/status/2026-09-02.md`).

## Done

- Governance: ADR-0009, the two-tier wall in AGENTS.md / legal.md, the `analyst-ghidra` skill, the spec
  template, the gate against decompiler output in `check_no_assets.py`, the Ghidra export scripts.
- Ghidra: the executable imported and auto-analysed (project in the ignored `re/`), inventory / strings /
  full decompilation exported for the analysts.

## Specifications (evening)

| Spec | Revision | Reviews | State |
|---|---|---|---|
| `spec-script-vm.md` | 3 | 14, 17, 21 (pending) | 241 natives settled, 12 excluded, scheduler waits for the sibling reviews |
| `spec-navigation.md` | 2 | 15, 20 (pending) | corner graph, sectors, doors, lifts, buildings; line of sight and sliding unread |
| `spec-ai-combat.md` | 2 | 16, 22 (pending) | perception, states, rails, combat rolls; figure thresholds excluded |
| `spec-movement-animation-camera.md` | 2 (in revision) | 18 | one frame clock (46.875 ms realised), camera, drawing order |
| `spec-campaign-camp-saves.md` | 3 | 19, 23 (pending) | campaign graph, camp, saves, statistics; production output formula excluded |

## In progress

- Codex review 13: the governance change.
- Analyst specifications (in order): script VM and natives; navigation (path graph, layers, doors, lifts);
  AI, stealth and combat; movement, animation and camera; campaign, the camp and saves; HUD and menus.

## Next

- Implementer batches from each reviewed spec; the `Assumption` registry shrinks with each; mission 1's
  intended route and every objective; each campaign mission won through play with a recorded replay; the camp;
  the full campaign; movies (licence decision pending: `docs/formats/movies.md`).
