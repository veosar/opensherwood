# Status report, 2026-09-02 (day 1)

Written for the maintainer, who follows the project on GitHub. Everything below is on `main`.

## Honest one-line summary

The engine is not a playable game yet: it boots into the original main menu (pictures, fonts and strings from
the player's files), Play! loads the first mission (`H01_Lin_VL`, Lincoln) behind its three briefing pages
with the camera on Robin, you can select Robin and walk him around obstacles with pathfinding, characters
animate with the real sprites (every NPC with the sprite the profile table assigns), guards walk their rail
programs, Escape opens the pause menu (continue, restart, quit with confirmation), the HUD frame is drawn at
the measured positions, credits play, music plays; the main menu and the briefing scene match the original's
captures (SSIM 0.9995 / 0.986 with the profile text and the parchment masked), and the whole flow also works
in the real window with OS-level mouse input. The first script VM runs the missions' scripts (ADR-0008): the
first mission's briefing pages, tutorial popups, objectives and messages come from its script (the
archery-training loop runs its sequences and messages; the shot itself is a stub); 73 natives are implemented,
95 are recorded stubs (five of them with the return value the stub policy requires), ids without a documented
row trap. Since 2026-09-03 soldiers see and hear the player characters (view cone, noise radius, alert
states with the alert animations) and Robin can knock a soldier out from behind (the script's "out of action"
predicate reads it); there is no sword fight, no bow, no damage, no options / profiles / saves. The format work that the whole port depends on is
largely done (see below), and the test harness plays the engine autonomously.

## What works (verified by tests and inspected captures)

| Area | State |
|---|---|
| Formats | Backgrounds, minimaps, UI archives, sprite bank (every frame), animation blocks, maps (occluders, walkable area, projection areas), missions (all chunks of all 39 files), script container, fonts. Specs in `docs/formats/`. |
| Engine core | Deterministic fixed-tick world, canonical hashing, snapshots with content identity (validated; VM snapshots are quiescent, one work budget per tick), replays with byte-accounted recording, walkable geometry (boundary + projection areas) with build-time navigation budgets (search work bounds in progress), A* pathfinding, sprite animation state, mission spec, NPC rail programs, the script VM (`vm.rs`, `natives.rs`) with objectives, texts, sequences, messages, activation and mission variables. |
| Rendering | CPU compositor: background + sprites in depth order (feet y) with per-sprite occluder masking + bitmap-font text; menus, briefing, pause menu, dialogs and HUD from the player's UI archive; winit/wgpu window, borderless fullscreen by default, F11. |
| Audio | Ogg Vorbis music per map / menu theme, PCM effects channel. |
| Scenarios | `menu` (default: the original main menu, Play! starts the campaign), `corridor` (synthetic), `map:<name>`, `mission:<name>` (37 of 39 load in strict mode; the two Sherwood missions need `OPENSHERWOOD_LENIENT_ASSETS=1`). |
| Harness | 54 test functions (22 synthetic on CI for Linux/Windows/macOS, 32 data-backed locally; mission replays on the session timeline, snapshot continuations, per-tick determinism), determinism fixture, oracle comparisons, `drive.py`, `play_window.py` (real mouse; keyboard through the RPC when the OS holds modifiers). |
| Reviews | Seven Codex reviews answered (`docs/decisions/reviews/`). The full review's P0 items and P1 items 7-17 are done (decode limits, SRES budgets, RPC drain, replay quotas, full content digest, geometry bounds, transactional restore, snapshot validation); P2-P4 are partly open. Review 4 (`2026-09-02-codex-review-4.md`) found the clean-room text sweep still incomplete and new High items; its disposition answers them, review 5 (`2026-09-02-codex-review-5.md`, verdict redesign for the replay time model and VM budgets) is answered in its disposition: replay time is now the session tick with screens in the timeline, native 32 is a barrier, 202 texts are non-modal; review 6 (`2026-09-02-codex-review-6.md`) found the VM budget scope, callback teardown, session checkpoints and a committed test artifact still wrong; its disposition answers them (one budget per tick, single teardown, checkpoints with screen digest and frame hash, artifact removed and rejected by the checker). Review 3's findings (copied text sweep, screens vs snapshots, UI decoding rule, typed `ui`, confirmations, transactional Play!, music) are addressed in its disposition. |

## What does not work yet

- Script VM: first version. 37 of 39 missions load with their script (the Sherwood hub and its outro are refused only by the script binding: `docs/formats/sherwood-hub.md` (2026-09-05) establishes their element index space and corrects the element order for every map, player slots at the tail, which re-binds eleven missions once implemented; `OPENSHERWOOD_LENIENT_ASSETS=1` runs them without a script); since 2026-09-03 none of the 37 traps: every native reached at load or in the first 300 ticks is implemented or a recorded stub (the stub policy of `docs/formats/scb.md`; all 37 run 300 strict ticks in the harness without a trap, fault or budget abort, none won or lost; ids without a documented row, such as 21, 40 and 179, still trap when a script reaches them). Sequences run concurrently (one per element, a hypothesis), scrolls are picked up within 24 px (a hypothesis) and fire `IsTaken`, `Hourglass` time is the world tick while waits assume 25 script ticks per second (units to be settled by an oracle trace), text pages are dismissed with ordinary input inside the replay timeline, stubs cover animations, doors, remarks, traps, buildings, banners, campaign flags and combat-related natives. Winning shows the won debriefing parchment, then the next level of the campaign graph starts (mission 1 -> the first secondary mission in Nottingham, whose script now runs its load-time callbacks); `CheckVictoryCondition` = 2 records `mission_lost`, which shows the lost parchment and returns to the main menu (no combat yet, so no mission can be lost through play; `debug.vm {"lose": true}` drives the flow test).
- AI: rail programs run (6 of 19 opcodes by inference, the rest no-ops); the stealth layer runs (`docs/original/stealth-and-combat.md` "Engine": view cone 45 degrees / 200 px, halved when crouched, a 150 px noise radius for running characters, noticed -> alarm -> search -> return with the soldiers' alert animations, the knock-out blow from behind with a timer scaled by the profile's resistance, natives 85 / 87 / 90 / 128 / 240 / 140 on real state, `ActionChange` to the scripts; every constant a hypothesis pinned by tests); no sight blocking by walls, no check-for scans, no fighting, no bow, no damage, no items, no quick-action icons.
- UI: movies are not implemented (profiles live in `profiles.json` under the artifact directory with the original's select / new / rename / delete screens, the main menu shows the selected one and Play! seeds its money; credits, load / save screens over the engine's own save files, and the options screens with the original's layout work; volumes reach the audio output, settings persist in `settings.json` under the artifact directory; the shortcut sets are shown but not rebindable) (their menu entries are drawn on the
  disabled plate); the HUD shows the script's money and the crouch / stand figures work; the map scroll opens the mini-map overlay (the level's `.min` picture with the camera's view, closed by a right click; the layout is the engine's until captured, `;` is not mapped yet); action icons and the clover counter are not implemented; the briefing lacks the character
  picture; the pause tint is an approximation.
- Profile-index -> sprite mapping done (`profile.cpf` SD / CV / PC tables; NPCs, civilians and `TOTO` actors wear their table sprite). Open: which hero fills a `SCOT` slot (campaign state; heroes are assigned in file order); the stat fields are read as hit points and knock-out resistance (hypotheses, `docs/formats/profile.md`); walk timing placeholder.
- Original path graph undecoded (we use our own grid over the boundary plus the projection areas, so the town maps are walkable); layers/sectors/doors/climbing not modelled (every area is flat).
- Save compatibility, video, Linux/macOS window testing, Android: not started.

## Decision needed from the maintainer

Commit `277af51` (the analyst's notes) put three verbatim briefing pages and other game text into the public
history; `main` no longer contains them, but the history does. Codex recommends deciding whether to rewrite the
public history (force push). That is destructive and was not done. See
`docs/decisions/reviews/2026-09-02-codex-full-review-disposition.md`.

## How to run

```
cargo run -p opensherwood-app --release -- --game-dir "<your GOG install>"        # main menu, Play! starts mission 1
cargo run -p opensherwood-app --release -- --scenario mission:EmbTut_FoC_EC --game-dir "<your GOG install>"
cargo run -p opensherwood-app --release -- --scenario map:nottingham --windowed
```

In the menu: click Play! (or use the arrows and Enter), Enter / the V seal pages through the briefing. In the
mission (the manual's rules): left click selects a character or orders the selected one to walk (around
obstacles), a double click runs, right click cancels the order or deselects, `c` / `s` crouch / stand, `F1` / `F5` quick save / quick load (plus rolling auto saves every minute), arrows /
screen edges scroll, F11 toggles fullscreen, Escape opens the pause menu (Escape again continues; Quit asks,
then returns to the menu).
`python harness/tools/play_window.py --flow menu` plays this flow with the real mouse. See `docs/build.md`.

## Recommended next steps (in order)

1. Oracle captures to replace the stealth / movement hypothesis constants; action icons, the mini-map's original presentation, multi-selection.
2. NPC behaviour: view cones and alarm stimuli, the remaining rail opcodes (oracle trace of one guard post).
3. Script VM: opcode semantics for the tutorial script (analyst work, then implementer), objectives and dialogues.
4. Combat, items, quick actions; then the campaign screen and saves.
5. Work through the open review findings (`docs/decisions/reviews/2026-09-02-codex-review-2-disposition.md`).
